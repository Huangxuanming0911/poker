services:
  - type: web
    name: texas-holdem-poker-ai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 8 --timeout 120 server:app
    autoDeploy: true
